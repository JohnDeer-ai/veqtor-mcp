{
  "name": "@veqtor/website",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.23.2 <23"
  },
  "scripts": {
    "dev": "ASTRO_TELEMETRY_DISABLED=1 astro dev",
    "preview": "ASTRO_TELEMETRY_DISABLED=1 astro preview",
    "audit": "npm audit --package-lock-only --audit-level=high --include=dev --include=optional",
    "build": "ASTRO_TELEMETRY_DISABLED=1 astro build && node scripts/check-site.mjs && node scripts/check-copy.mjs",
    "check": "ASTRO_TELEMETRY_DISABLED=1 astro check",
    "test": "node --test scripts/*.test.mjs"
  },
  "dependencies": {
    "@fontsource-variable/inter": "5.2.8",
    "@fontsource-variable/roboto-mono": "5.2.9",
    "astro": "7.2.8"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.9",
    "parse5": "7.3.0",
    "typescript": "6.0.3"
  }
}
